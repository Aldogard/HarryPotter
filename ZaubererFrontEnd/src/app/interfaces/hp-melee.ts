export interface HpMelee {
    id: number,
    name: string,
    energyUsage: number,
    descriptionOption: string,
    maxDamage: number,
    requiredExperience: number,
    energyRecovery: number,
    stunned: boolean,
    confounded: boolean,
}
